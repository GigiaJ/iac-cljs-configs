ui:
  enabled: true

server:
  standalone:
    enabled: true
  readinessProbe:
    enabled: true
    path: "/v1/sys/health"
  ha:
    enabled: false

  dataStorage:
      enabled: true
      size: 2Gi
      storageClass: "hcloud-volumes"
  
  nodeSelector:
    location: "de"
